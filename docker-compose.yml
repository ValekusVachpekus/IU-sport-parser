services:
  bot:
    build: .
    restart: always
    mem_limit: 256m
    env_file: .env
    volumes:
      - ./data:/app/data
    # The site interprets schedule times in Moscow time; keep the container in sync.
    environment:
      - TZ=${TZ:-Europe/Moscow}
